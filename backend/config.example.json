{
  "listen_addr": "127.0.0.1:8080",
  "repo_root": "..",
  "work_dir": ".runs",
  "timeout_seconds": 120,
  "max_concurrent_runs": 1,
  "forge_bin": "forge",
  "rpc_urls": {
    "mainnet": "${MAINNET_RPC_URL}",
    "base": "${BASE_RPC_URL}",
    "arbitrum": "${ARBITRUM_RPC_URL}"
  }
}
